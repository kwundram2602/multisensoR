% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{check_scaling}
\alias{check_scaling}
\title{Check and print the value scaling of input GeoTIFF pairs}
\usage{
check_scaling(pairs, expected_min = 0, expected_max = 1, sample_frac = 0.01)
}
\arguments{
\item{pairs}{data.frame returned by [find_l8_s2_pairs()].}

\item{expected_min}{Numeric. Expected minimum value. Default `0`.}

\item{expected_max}{Numeric. Expected maximum value. Default `1`.}

\item{sample_frac}{Numeric in `(0, 1]`. Fraction of pixels to sample per
band (speeds up large images). Default `0.01` (1%).}
}
\value{
A data.frame with columns `pair_id`, `sensor`, `band`, `min`, `max`
  (invisibly). Printed as a formatted table.
}
\description{
Computes per-band min/max for each file in `pairs` and prints a summary
table. Warns if values fall outside the expected `[expected_min, expected_max]`
range (default `[0, 1]`.
}
