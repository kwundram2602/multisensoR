% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{mask_nodata_pair}
\alias{mask_nodata_pair}
\title{Apply a shared NoData mask to an aligned L8 / S2 pair}
\usage{
mask_nodata_pair(
  l8_path,
  s2_path,
  out_l8 = sub("\\\\.tif$", "_masked.tif", l8_path, ignore.case = TRUE),
  out_s2 = sub("\\\\.tif$", "_masked.tif", s2_path, ignore.case = TRUE),
  zero_as_nodata = TRUE
)
}
\arguments{
\item{l8_path}{Character. Path to the aligned Landsat-8 GeoTIFF.}

\item{s2_path}{Character. Path to the Sentinel-2 GeoTIFF.}

\item{out_l8}{Output path for the masked L8 file. Defaults to
`<basename>_masked.tif` next to the input.}

\item{out_s2}{Output path for the masked S2 file. Defaults to
`<basename>_masked.tif` next to the input.}
}
\value{
A named list with elements `l8` and `s2` (output paths), invisibly.
}
\description{
A pixel is set to `NA` in **both** images if it is `NA` in **either** image
(union of NoData masks).
}
\details{
Requires both rasters to already share the same grid (run [align_l8_to_s2()]
first).
}
