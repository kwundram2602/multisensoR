% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R
\name{landsat_sentinel_dataset}
\alias{landsat_sentinel_dataset}
\title{Landsat-8 / Sentinel-2 Patch Dataset}
\usage{
landsat_sentinel_dataset(
  landsat_files,
  sentinel_files,
  patch_size = 256L,
  stride = NULL,
  augment = FALSE
)
}
\arguments{
\item{landsat_files}{Character vector of paths to (aligned) Landsat-8 GeoTIFFs.}

\item{sentinel_files}{Character vector of paths to Sentinel-2 GeoTIFFs
(same length and order as `landsat_files`).}

\item{patch_size}{Side length of square patches in pixels. Default `256`.}

\item{stride}{Step size between patch origins. Default equals
`patch_size` (non-overlapping tiles). Set smaller for more patches.}

\item{augment}{If `TRUE`, apply random horizontal and vertical flips.}
}
\value{
A `torch::dataset` object usable with [torch::dataloader()].
}
\description{
A `torch` dataset that reads co-registered, aligned Landsat-8 and Sentinel-2 pairs.
}
\details{
**Assumptions:**
- Both files are on the **same pixel grid** (same CRS, resolution, extent).
  Use [preprocess_pairs()] to align L8 (30 m) to the S2 (10 m) grid first.
- Pixel values are already scaled to reflectance **[0, 1]**.
- Both sensors export 6 bands in matching order:
  Blue, Green, Red, NIR, SWIR1, SWIR2.
}
