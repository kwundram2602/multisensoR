% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_unet}
\alias{predict_unet}
\title{Predict Sentinel-2 from a Landsat-8 image using a trained U-Net}
\usage{
predict_unet(
  model_path,
  landsat_path,
  out_path = NULL,
  in_channels = 6L,
  out_channels = 6L,
  patch_size = 256L,
  device = NULL
)
}
\arguments{
\item{model_path}{Path to a `.pt` checkpoint written by [train_unet()].}

\item{landsat_path}{Path to a Landsat-8 GeoTIFF whose pixel values are
scaled to reflectance \[0, 1\].}

\item{out_path}{Output path for the predicted GeoTIFF.}

\item{in_channels}{Number of input channels the model was trained with.
Default `6L`.}

\item{out_channels}{Number of output channels. Default `6L`.}

\item{patch_size}{Patch side length in pixels (must match training).
Default `256L`.}

\item{device}{`"cpu"` or `"cuda"`. Defaults to CUDA if available.}
}
\value{
A [terra::SpatRaster] with `out_channels` layers and the same
  extent / CRS as the input. Pixel values are in \[0, 1\] (reflectance).
  Pixels not covered by any full patch are `NA`.
}
\description{
Predict Sentinel-2 from a Landsat-8 image using a trained U-Net
}
