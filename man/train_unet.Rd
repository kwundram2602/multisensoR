% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{train_unet}
\alias{train_unet}
\title{Train the U-Net on Landsat-8 → Sentinel-2 image translation}
\usage{
train_unet(
  model,
  train_dl,
  epochs = 10L,
  optimizer = NULL,
  val_dl = NULL,
  device = NULL,
  checkpoint_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{model}{An `nn_module` returned by [unet_model()].}

\item{train_dl}{A `torch::dataloader` whose items contain `$landsat`
and `$sentinel` tensors (produced by [landsat_sentinel_dataset()]).}

\item{epochs}{Integer. Number of training epochs. Default `10`.}

\item{optimizer}{An `torch` optimiser instance.}

\item{val_dl}{Optional validation `dataloader` .}

\item{device}{A `torch_device` or character string (`"cpu"`, `"cuda"`).}

\item{checkpoint_dir}{Character or `NULL`. If a directory path is given,
the model state is saved as `unet_epoch_<N>.pt` after every epoch.}

\item{verbose}{Logical. Print per-epoch progress? Default `TRUE`.}
}
\value{
A `data.frame` with columns `epoch`, `train_loss`, and (if
  `val_dl` is provided) `val_loss`. Returned invisibly.
}
\description{
Runs a supervised training loop: Landsat-8 patches are the model input
(predictors) and Sentinel-2 patches are the reconstruction target.
Loss is L1 (pixel-wise mean absolute error).
}
