% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{preprocess_pairs}
\alias{preprocess_pairs}
\title{Preprocess all image pairs}
\usage{
preprocess_pairs(
  pairs,
  out_dir = NULL,
  scale_l8 = c(gain = 2.75e-05, offset = -0.2),
  scale_s2 = c(gain = 1/10000, offset = 0),
  mask_nodata = TRUE,
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{pairs}{data.frame returned by [find_l8_s2_pairs()].}

\item{out_dir}{Output directory. Defaults to `preprocessed_pairs/` next
to the L8 input files.}

\item{scale_l8}{Named numeric vector `c(gain, offset)` applied as
`gain * DN + offset` to scale L8 to \[0, 1\].
Default: `c(gain = 0.0000275, offset = -0.2)` (Landsat C2 L2 formula).
Set to `NULL` to skip scaling.}

\item{scale_s2}{Named numeric vector `c(gain, offset)` for S2.
Default: `c(gain = 1/10000, offset = 0)` (divide by 10 000).
Set to `NULL` to skip scaling.}

\item{mask_nodata}{Logical. Apply shared NoData mask? Default `TRUE`.}

\item{overwrite}{Logical. Re-process existing files? Default `FALSE`.}

\item{...}{Additional arguments forwarded to [align_l8_to_s2()].}
}
\value{
A data.frame with columns `l8`, `s2` (processed paths), and `l8_orig`.
}
\description{
Pipeline per pair:
1. **Scale** – apply `gain * x + offset` to bring values to \[0, 1\]
2. **Align** – resample L8 onto the S2 pixel grid ([align_l8_to_s2()])
3. **Mask**  – harmonise NoData between sensors ([mask_nodata_pair()])
}
\examples{
\dontrun{
pairs <- find_l8_s2_pairs("data/L8/", "data/S2/")

# default: L8 C2 L2 formula + S2 / 10000
pairs_proc <- preprocess_pairs(pairs)

# custom scaling
pairs_proc <- preprocess_pairs(pairs,
  scale_l8 = c(gain = 0.0000275, offset = -0.2),
  scale_s2 = c(gain = 1/10000,   offset = 0))

# no scaling (data already in [0,1])
pairs_proc <- preprocess_pairs(pairs, scale_l8 = NULL, scale_s2 = NULL)
}
}
